




original images and not preprocessed are hashed.
If user wants to use other preprocessing, there is a prompt asking

image found -> [continue, re-compute]


Tables:

Inference:
    Myotube images: hash -> myotubes object
    Nuclei images: hash -> nucleis objects

    image_state: hash -> state dict containing pre-processing steps.

Validation:
    hash -> validation state
